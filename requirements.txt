# Core MCP client library (Python SDK)
mcp>=1.0.0
# NOTE: alpaca-mcp-server is NOT a pip package.
# It is invoked at runtime via `uvx alpaca-mcp-server` (install uvx separately).
# Ensure `uvx` is available on PATH in your environment / GitHub Actions runner.

# LLM & AI
google-genai>=0.1.1
pydantic>=2.0.0

# Web Research
firecrawl-py>=1.0.0
requests>=2.31.0

# Database
supabase>=2.0.0

# Data Analysis & Math
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0

# Dashboard
streamlit>=1.30.0

# Utility & Config
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
